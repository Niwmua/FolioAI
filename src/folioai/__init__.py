"""folioai: a faithful book translation pipeline.

PDF in, structured intermediate representation, LLM translation, second-model verification,
and export to Markdown, EPUB, PDF, DOCX, HTML or plain text.

The invariant everything else serves: block count in equals block count out.
"""

from __future__ import annotations

__version__ = "0.1.0"
__all__ = ["__version__"]
